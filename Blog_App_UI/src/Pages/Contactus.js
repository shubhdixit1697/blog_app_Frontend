import Base from "../Components/Base";

const Contactus=()=>{

    return(
        <Base>
        
        <div>
        <h1>We are not reachable at the moment</h1>
        </div>
        
        </Base>
    );
};

export default Contactus;